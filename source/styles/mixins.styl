size(w, h = w)
  width w
  height h

circle(d)
  width d
  min-width d
  height d
  min-height d
  border-radius d * 0.5

max-size(w, h = w)
  max-width w
  max-height h

min-size(w, h = w)
  min-width w
  min-height h
